package LevelEditor;

import Utils.Colors;

import javax.swing.*;

public class MovementPermissionBuilder extends JPanel {
    public MovementPermissionBuilder() {
        setBackground(Colors.MAGENTA);
        setLocation(0, 0);
        setSize(585, 562);
        setAutoscrolls(true);
    }
}
