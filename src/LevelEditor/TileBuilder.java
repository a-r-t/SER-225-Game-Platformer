package LevelEditor;

import Utils.Colors;

import javax.swing.*;

public class TileBuilder extends JPanel {
    public TileBuilder() {
        setBackground(Colors.MAGENTA);
        setLocation(0, 0);
        setSize(585, 562);
        setAutoscrolls(true);
    }
}
